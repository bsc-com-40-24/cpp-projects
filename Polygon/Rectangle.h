#pragma once
#include "Polygon.h"
class Rectangle : public Polygon{
  public: 
  Rectangle(int width, int height);
   // mWidth=width;
    //mHeight=height;
  
    int Area();
      //return mWidth * mHeight;
   // }
};